// Inserieren im Meeko-Stil (20.09.2026). Attrappe: speichert nichts. Nicht verlinkt, nicht indexiert.
import MeekoInserieren from './MeekoInserieren'

export const metadata = { title: 'Labor: Meeko, Inserieren', robots: { index: false, follow: false } }

export default function Page() {
  return <MeekoInserieren />
}
