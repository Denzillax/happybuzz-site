// Vorschau im Look von craft.wild.as mit dem Pixel-B als Logo (19.09.2026). Nicht verlinkt, nicht indexiert.
import WildLabor from './WildLabor'

export const metadata = { title: 'Labor: Wild', robots: { index: false, follow: false } }

export default function Page() {
  return <WildLabor />
}
