# Bewerben v3 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Tester-Bewerbung abschaffen (alle Angemeldeten sind Tester), eine Mitarbeiter-Stelle pro Person, Absage mit Glocke, /beta mit menschlicher Erklär-Sektion.

**Architecture:** Kleine Migration (Status 'abgesagt' + One-Open-Index), Umbau /bewerben und OverviewTab, Hero-CTAs, /beta-Intro. Verifikation NUR live im Preview, KEIN npm run build.

**Tech Stack:** Next.js 14, Supabase (applications-Tabelle), MCP apply_migration (Einzel-Statements bei execute_sql!).

---

### Task Q1: Migration

**Files:** Create `supabase/migrations/20260816_applications_v3.sql`

- [ ] Check-Constraint droppen + neu mit ('neu','erledigt','abgesagt'); partieller Unique-Index `applications_one_open on applications(user_id) where status='neu'`. Via apply_migration einspielen, Datei committen. Commit `feat(bewerben): Status abgesagt + eine offene Bewerbung pro Person (DB)`.

### Task Q2: /bewerben

**Files:** Modify `src/app/(public)/bewerben/page.jsx`

- [ ] TESTER-Karte raus, Hinweisbox "schon Tester" (Link /beta), Kartenzustände neu/erledigt/abgesagt/gesperrt laut Spec, Insert-23505 still. Live prüfen. Commit `feat(bewerben): nur noch Mitarbeiter, eine Stelle pro Person, Absage-Zustand`.

### Task Q3: Admin Absagen + Hero

**Files:** Modify `src/components/admin/tabs/OverviewTab.jsx`, `src/components/admin/useAdminData.jsx`, `src/components/home/Hero.tsx`

- [ ] rejectApplication-Handler (status abgesagt + Glocke "Leider müssen wir dir mitteilen ..." + Audit application_rejected), Knopf "Absagen" auf der Karte. Hero-Beta-CTAs: "So funktioniert die Beta" -> /beta, "Mitarbeiter werden" -> /bewerben, Text angepasst. Live prüfen. Commits `feat(admin): Bewerbung absagen mit Benachrichtigung` und `fix(hero): Beta-CTAs ohne Tester-Bewerbung`.

### Task Q4: /beta + Abschluss

**Files:** Modify `src/app/(public)/beta/page.jsx`

- [ ] Erklär-Sektion oben (Humor, Katalog-Look); Checklisten-Punkte pg_hero_beta_dwell / pg_bewerben_* auf v3 korrigieren + Punkt Erklär-Sektion. Voll-E2E als Denis (bewerben, sperren, absagen, Glocke, Slot frei), Testdaten löschen, npm test. Commits `feat(beta): Erklär-Sektion in einfachen Worten` und `test(beta): Bewerben-v3-Checkpunkte`.
